module Lists.Recursion where

import Prelude hiding (sum,all,reverse,
                       prod,append,take,
                       length,concat,drop,
                       elem,map,takeWhile,
                       and,filter,dropWhile,
                       or,init,minimum,
                       any,last,maximum)

length = undefined

sum = undefined

prod = undefined

last = undefined

init = undefined

elem = undefined

and = undefined
or = undefined

any = undefined

all = undefined

maximum = undefined

minimum = undefined

append = undefined

concat = undefined

take = undefined

drop = undefined

takeWhile = undefined

dropWhile = undefined
     
map = undefined

filter = undefined
  
reverse = undefined

partition = undefined
